# Project Memory

Transferable memory for the `llm-wiki` wiki, exported from auto-memory so it travels across machines. Treat entries as point-in-time observations — verify against current code/files before asserting as fact.

This file is currently empty. Stable, transferable behavioural or feedback rules are appended here as `## Topic — description` sections once they have proven durable; rules about wiki structure or schema go to `CLAUDE.md` instead. See `CLAUDE.md` → Memory tiers for the graduation path.
