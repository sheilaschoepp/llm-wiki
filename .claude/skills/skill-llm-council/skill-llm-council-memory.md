---
type: memory
skill: skill-llm-council
updated: 2026-07-14
---

# Skill-llm-council memory

Corrections, rewrites, and scope adjustments specific to the `skill-llm-council` skill. Read at the start of every skill-llm-council operation.

Cross-skill rules live in `.claude/skills/multi-skill/multi-skill-memory.md` — read that file too.

Newest entry on top, one entry per heading.

## 2026-07-31 — query grounds held but all coherent joints failed entailment

One revisit signal fired: every locator refuter held, but entailment refuters rejected all four coherent load-bearing joints. The repeated shape was exact-text incompleteness or overreach: a new status rule lacked its verification check; grounded `none` contradicted an unchanged source-naming mandate; a same-version fix replaced triage with unconditional application; and a collision guard was check-then-create rather than atomic. Run one exact-text whole-set semantic test over both changed and unchanged adjacent instructions before refuters, including concurrency semantics. The run also exhausted fresh-agent capacity once, so reserve the complete refuter budget before Step 2 or persist a machine-readable meta-chair continuation packet.

## 2026-07-30 — lint independence restart and entailment refuters rejected five of seven joints

Two revisit signals fired. An inherited-context advisor batch had to be discarded and rerun with blank-context advisors; make blank-context launch mode machine-checked for independence-sensitive agents. Entailment refuters rejected five of seven load-bearing joints, all for over-broad final wording despite sound grounds: synthetic report counts contradicted script-count invariants; bare anchor search ignored page-local IDs; sorter link cardinality counted unrelated outputs; the description lost triggers; and the AI threshold allowed punctuation-only stacks. Require exact final replacement text and one pre-refuter whole-set semantic check.

## 2026-07-28 — checkup councils converged; locator prompt misfired and semantic reread reverted one held edit

Three revisit signals fired on the checkup run. Both councils converged on the same central orchestration defects, so the two-council split under-earned its cost on the core slate. The locator refuter then rejected every candidate because the proposed replacement was not already on disk — a temporal category error that required the escalation refuter to settle — so the locator prompt must state explicitly that proposed text is expected to be absent before application. Finally, an edit held by coherence, entailment, and escalation still needed rollback because its exact held subset left an adjacent contradictory sentence; this independently repeats the audit-run signal for one fresh final-subset semantic refuter after paired adjudication and before writing.

## 2026-07-28 — audit councils converged but held-subset semantics invalidated two edits

Two revisit signals fired on the audit run. Both councils independently converged on the same core defects — unsafe unknown arguments, raw-byte identity, terminal page writes, claim-level proof, interruption recovery, preservation, and `updated:` scope — so the two-council split under-earned its cost on the central change-set. After whole-set coherence and paired refuters, two initially held and applied candidates still needed rollback on the required semantic reread: routing metadata conflicted with unchanged `When To Invoke`, and raw-byte baselines depended on the separately refuted terminal-report state. Upgrade the gate by running one fresh final-subset semantic refuter after paired verdicts and before any write; selecting a held subset can break dependencies that appeared coherent in the larger packet.

## 2026-07-28 — self-run councils collapsed and entailment refuters rejected three of four clusters

Two revisit signals fired in the self-review. Both councils converged on the same four operational defects (chair-input persistence, exact final edit text, whole-set coherence, and non-convergence); Council 1 fully collapsed and Council 2 retained only trigger/structure side findings, so the two-council split under-earned its cost on the core change-set. Entailment refuters then rejected three of four load-bearing clusters: prompt isolation overreached its cited untrusted-import ground and conflicted with the canonical role-prompt assembly; the chair-packet edit improperly bundled an output-shape requirement; and the coherence edit improperly bundled a repair exception. One authorized repair still failed whole-set coherence, so those three clusters were terminally demoted. Only the non-convergence and report-disposition cluster held. This run reinforces the need for an independent whole-change-set gate before paired refuters and for reporting each council's unique surviving contribution.

## 2026-07-27 — refuters refuted ALL THREE load-bearing edits; every failure shared one shape (audit gate-reduction review)

Change-review run on audit's certifying refuter gate. Three signals. (1) The over-reach signal fired at its maximum: 3 of 3 load-bearing edits refuted, including one that all ten advisors AND both chairs converged on unanimously — the claim that the new adjudication layer could not express an absence refutation. A single refuter reading `verification.md:45` showed the structured-verdict rule asks only for "the verbatim quote it read", never a quote contradicting the claim, so the route was never blocked; and the proposed fix would have let a refuter veto any certification by fabricating a quote (a discarded finding would still block). The Worked Example the council called "unexecutable" executes as written. Consensus predicted ground-truth survival *inversely* here. (2) All three refuted edits shared one shape: reasoning from the change's summary rather than the file, then proposing a fix whose own next clause contradicted it (the removal-precondition edit conceded the locator refuter is page-scoped one clause after demanding it answer a whole-raw question, making the gate unreachable). A cheap coherence pass — one agent reading each edit against the file it cites, before refuters spawn — would have killed all three at a fraction of the cost. This is the second run to want that pass; consider making it Step 5.5. (3) Orchestrator error: a chair was spawned pointing at an advisor-response file not yet written, so Chair 2 ran on 4 of 5 inputs. Persist and existence-check every advisor response before spawning any chair.

## 2026-07-26 — meta-chair hybrid refuted; six-round non-convergence forced the alternative to the surface (gate1 attachment change-review run)

Three lessons from the change-review run on the paired-source-attachment design. (1) The meta-chair fused the two chairs' conflicting settlement designs into a hybrid (definitional meaning + Tier-3 floor) and built the whole change-set around it; a refuter killed the hybrid on ground truth neither chair had checked (tiers are per-run impact judgements recorded nowhere in page state, and authoring skills are forbidden the labels) — when chairs conflict on a mechanism, resolve to one chair's design intact, or refute the hybrid mechanism alone before building a change-set around it. (2) The repaired change-set carried in-tranche contradictions between its own items that only overlapping refuter clusters caught — a cheap coherence pass (one agent reading only the change-set for internal consistency) between repair and re-refutation would be cheaper than the refuter round that finds them. (3) Six refuted rounds at one design joint is a design-smell threshold worth codifying: force the strongest structural alternative to the user instead of another repair; this run surfaced the alternative (preserve the hash through demotion) only because a chair happened to elevate it.

## 2026-07-26 — refuters refuted a majority of load-bearing edits (joint ingest+audit+verification.md run)

First-round refuters refuted 9 of 15 load-bearing edit clusters (three needed second or third rounds; two council-endorsed mechanisms died outright on ground truth). The councils over-reached and the refuter gate carried the run — the revisit signal the skill documents. Same run: a described-but-untexted edit twice consumed a refuter round before being noticed; consider requiring verbatim old → new text on every final-set edit before refuters spawn.
